# include "main.h"
/**
 * main - Entry point
 * Description: 'function that checks for lowercase character.'
 * 
 * Returns Always 0 (Success)
 */
int main(void)
{
int c;
if (c >= 'a' && c <= 'z')
	return (1);
else
	return (0);
}

