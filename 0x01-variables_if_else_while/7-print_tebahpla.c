#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int letter;
for (letter = 'z'; letter <= 'a'; letter++)
putchar(letter);
return (0);
}
