#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0.
 */
int main(void)
{
int letter;
for (letter = ‘z’; letter >= ‘a’; letter–)
(letter = ‘z’; letter >= ‘a’; letter–)
return (0);
}
